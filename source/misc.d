/*char cmd;
string arg;
do
{
    arg = readln;
    cmd = arg.toLower[0];
    arg.popFront;
    switch(cmd)
    {
        case '>':
            if(!s.moveNext)
                writeln("End of file.");
            break;
        case '<':
            if(!s.movePrev)
                writeln("Start of file");
            break;
        case 'r':
            writeln(s.getInfo);
            break;
        case 'q':
            writeln("Exiting...");
            break;
        case 'l':
            writeln(s.printLineStack);
            break;
        default:
            writeln("who knows");
            break;
    }
} while(cmd != 'q');
*/
